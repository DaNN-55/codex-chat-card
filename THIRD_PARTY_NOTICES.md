# 第三方软件说明

本项目通过 npm 安装第三方依赖，各依赖继续适用其各自的许可证。本文件用于集中告知这些第三方组件及其许可证，不替代项目自身的 [MIT License](LICENSE)，也不替代第三方许可证原文。

直接依赖包括：

| 软件包 | 许可证 |
| --- | --- |
| `react`、`unified`、`remark-*`、`emoji-regex` | MIT |
| `smol-toml` | BSD-3-Clause |
| `satori`、`@resvg/resvg-js` | MPL-2.0 |
| `@fontsource/*` 字体包 | OFL-1.1 |

开发依赖包括：

| 软件包 | 许可证 |
| --- | --- |
| `@types/*` | MIT |
| `typescript` | Apache-2.0 |

准确版本以 `package-lock.json` 为准，完整许可证文本以各已安装软件包附带的文件为准。
